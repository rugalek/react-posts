import React from 'react';

export class FbCard extends React.Component {
    render() {
        const {avatar, name, education, text, likes, replies, online} = this.props;
        return <div>

            
        </div>
    }
}